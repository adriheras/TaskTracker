<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.listadetareas.MainActivity">

    <item
        android:id="@+id/action_add_task"
        android:icon="@drawable/ic_add_task"
        app:showAsAction="always"
        android:title="Add task"/>

    <item
        android:id="@+id/action_delete_task"
        android:icon="@drawable/ic_delete_all_tasks"
        app:showAsAction="always"
        android:title="Delete tasks"/>

    <item
        android:id="@+id/action_menu"
        android:icon="@drawable/ic_menu"
        app:showAsAction="always"
        android:title="Menu">
        <menu>
            <item
                android:id="@+id/action_search_task"
                android:title="Search tasks" />
            <item
                android:id="@+id/action_export_tasks"
                android:title="Export tasks"/>
            <item
                android:id="@+id/action_import_tasks"
                android:title="Import tasks" />
            <item
                android:id="@+id/action_settings"
                android:title="Configuration" />
        </menu>
    </item>
</menu>
